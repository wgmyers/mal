;; rev.mal
;; Reverse a list

(def! rev
  (fn* (xs)
    (if (= (count xs) 1)
        xs
        (concat 
           (rev (rest xs))
           (list (first xs))))))
           
;; main
(println (rev (list 1 2 3 4 5 6)))
